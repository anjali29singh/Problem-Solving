#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int N, K;
    cin >> N >> K;
    ll arr[N];
    for (int i = 0; i < N; i++)
    {
        cin >> arr[i];
    }

    ll ans = 0, cnt = 0;

    for (int i = 0; i < N; i++)
    {
    }

    return 0;
}